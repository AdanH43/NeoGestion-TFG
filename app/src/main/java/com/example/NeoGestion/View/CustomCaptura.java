package com.example.NeoGestion.View;

//esta clase esta vacia porque es una tonteria que te pide la libreria del escaner para que se gire la camara con el movil
// ya que al crear una activity puedo meter personalizaciones en el manifest ahi puedes ver para que sirve esta clase

import android.app.Activity;
import android.content.Intent;

import com.journeyapps.barcodescanner.CaptureActivity;

public class CustomCaptura  extends CaptureActivity {
}
